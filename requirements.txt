# ── Web framework ─────────────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart          # multipart file upload support for FastAPI

# ── Document extraction ───────────────────────────────────────────
pymupdf                   # PDF text layer (fitz) — faster & more robust than pypdf
pdf2image                 # PDF → image for OCR fallback
pytesseract               # OCR engine wrapper (tesseract must be installed)
Pillow                    # image handling
python-docx               # .docx / .doc  (NEW)
openpyxl                  # .xlsx / .xls  (NEW)
python-pptx               # .pptx / .ppt  (NEW)
# csv, html.parser — stdlib, no install needed

# ── Embeddings ────────────────────────────────────────────────────
sentence-transformers     # multilingual-e5-small + cross-encoder reranker (auto-downloads on first run)
torch                     # sentence-transformers runtime
torchvision
numpy
rank_bm25                 # BM25 lexical scoring for hybrid search

# ── LLM ───────────────────────────────────────────────────────────
google-genai              # Gemini API client (streaming support)
python-dotenv             # loads GOOGLE_API_KEY from .env

# ── Auth / DB ─────────────────────────────────────────────────────
bcrypt                    # password hashing
# sqlite3 — stdlib

# ── Testing (dev only) ──────────────────────────────────────────────
pytest
httpx                     # FastAPI TestClient dependency
